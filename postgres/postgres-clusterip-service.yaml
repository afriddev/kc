apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: his-keycloak
  labels:
    app: keycloak
    component: postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app: keycloak
    component: postgres